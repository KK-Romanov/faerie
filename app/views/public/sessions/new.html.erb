 <!--<header>-->
    <style>
      #intro {
        background-image: url(https://mdbootstrap.com/img/new/fluid/city/008.jpg);
        height: 100vh;
      }

      /* Height for devices larger than 576px */
      @media (min-width: 992px) {
        #intro {
          margin-top: -58.59px;
        }
      }

      .navbar .nav-link {
        color: #fff !important;
      }
    </style>

    <!-- Navbar使用検討 -->
    <!--<nav class="navbar navbar-expand-lg navbar-dark d-none d-lg-block" style="z-index: 2000;">-->
    <!--  <div class="container-fluid">-->
    <!--  </div>-->
    <!--</nav>-->
    <!-- Navbar使用検討 -->

    <!-- Background image -->
    <div id="intro" class="bg-image shadow-2-strong">
      <div class="mask d-flex align-items-center h-100" style="background-color: rgba(0, 0, 0, 0.8);">
        <div class="container">
          <div class="row justify-content-center">
            <div class="col-xl-5 col-md-8">
              <div class="bg-white  rounded-5 shadow-5-strong p-5">
                <div class = "text-center">
                 <h4><strong>会員ログイン</strong></h4>
                  </div>
                  <%= form_with model: resource, url: user_session_path do |f| %>
                    <div class="field">
                      <%= f.label :メールアドレス %><br />
                      <%= f.email_field :email, autofocus: true, autocomplete: "email", size: 34 %>
                    </div>
                    <br>
                    <div class="field">
                      <%= f.label :パスワード %><br />
                      <%= f.password_field :password, autocomplete: "current-password", size: 34 %>
                    </div>
                    <br>
                    <% if devise_mapping.rememberable? %>
                      <div class="field">
                        <%= f.check_box :remember_me %>
                        <%= f.label :remember_me %>
                      </div>
                    <% end %>
                  
                    <div class="actions">
                      <%= f.submit "ログイン", class: "btn btn-primary btn-block" %>
                    </div>
                  <% end %>
                  <br>
                  会員登録済みでない方<br>
                  <%= link_to "こちら", new_user_registration_path %>から新規登録を行ってください。

                <!-- Email input -->
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- Background image -->
    <hr class="m-0" />